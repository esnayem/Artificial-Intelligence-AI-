One hot encoding
'Same' -> same size after this
'Valid' -> small size after this
mobile net a "plot_model" necessary

TrainingSet --> tuning parameters
Validation Set --> tuning hyper-parameters
TestSet --> evaluating model's performance

one hot encoding --> softmax

